import { FRONTEND_URL } from '@/misc/environment';

export const Info = () => (
  <div>
    <div className="text-[1.2em] text-center m-2">
      Open the job listing that you want to apply for and click the select job description button.
      <br />
      You can set automatic cover letter and CV generation in the settings.
    </div>
  </div>
);
